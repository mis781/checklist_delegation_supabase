// The inbox now reads live data from Supabase (see ../services/whatsappApi.js).
// Only the local "who is the logged-in agent" placeholder remains here since
// there's no agent-session table yet.

export const activeAgent = { id: "agent_01", name: "Whatsapp Dash" };
